import React from 'react';

const AdminDashboard = () => (
  <div>
    <h1>Dashboard Admin</h1>
    <p>Thống kê tổng quan, số lượng user, đơn hàng, sản phẩm...</p>
    {/* Bạn có thể fetch dữ liệu từ API dashboard ở đây */}
  </div>
);

export default AdminDashboard;
